import { Routes } from '@angular/router';
import { InterestComponent } from './interest.component';

export const INTEREST_ROUTERS: Routes = [
  {
    path: '',
    component: InterestComponent,
  },
];
